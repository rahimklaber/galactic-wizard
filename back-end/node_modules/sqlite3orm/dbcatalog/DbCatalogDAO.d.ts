import { SqlDatabase } from '../core';
import { DbTableInfo } from './DbTableInfo';
export declare class DbCatalogDAO {
    sqldb: SqlDatabase;
    constructor(sqldb: SqlDatabase);
    readSchemas(): Promise<string[]>;
    readTables(schemaName: string): Promise<string[]>;
    readTableInfo(tableName: string, schemaName?: string): Promise<DbTableInfo | undefined>;
    protected callSchemaQueryPragma(pragmaName: string, identifierName: string, identifierSchema: string): Promise<any[]>;
    static getTypeAffinity(typeDef: string): string;
}
